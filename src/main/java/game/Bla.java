package game;

public class Bla {

    public static void main(String[] args) {
        System.out.println("hallo");

/*        Random random = new Random();
        List<Integer> niceList = new ArrayList<>();
        List<Boolean> flagList = new ArrayList<>();
        for (int i = 0; i < 20; i++) {
            niceList.add(random.nextInt());
            flagList.add(random.nextBoolean());
        }
        System.out.println(niceList.toString());
        System.out.println(flagList.toString());*/
/*        System.out.println("here we go");
        List<Integer> a = Arrays.asList(0, 0, 1);
        List<Integer> b = Arrays.asList(1, 0, 0);
        Set<List> mySet = new HashSet<>();
        mySet.add(a);
        mySet.add(b);
        System.out.println(mySet.size());*/
    }
}
